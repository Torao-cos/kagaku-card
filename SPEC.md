# 元素記号・化学式カード — 仕様書（正本）

最終更新: 2026-09-20（v1.2: 「覚えた」ボタン1つ・タップ/左スワイプ＝×・×は基準値に戻す・カード一覧から1枚ずつ戻す）／ 合意者: owner ／ 状態: 公開中 https://kagaku-card.pages.dev/

> 中学生向けの暗記用フラッシュカード。クイズではない。赤シートのように「答えを隠して→タップで開く」だけの学習加速ツール。
> 評価は授業のテストで行う。アプリは成績を集計しないし、サーバも持たない。

---

## 1. 目的と割り切り

- **目的**: 元素記号・化学式を「見てすぐ言える」状態にする。取り組むハードルを最小化する（電車内・スキマ時間で気軽に）。
- **完結させない**: このアプリだけで学習を完結させる設計ではない（owner方針）。書く練習・テストは授業側。
- **管理しない**: 進捗は各生徒の端末（localStorage）のみ。サーバ・ログイン・集計なし。
- **配布**: URL1本を配る。認証・合言葉なし（データは事実情報で著作権問題なし）。
- **対象**: ownerの担当生徒＋隣のクラス。発展として一般公開あり。

## 2. データ

### 2.1 正本 = `data/cards.csv`（唯一の編集箇所）

```csv
category,level,name,symbol,note
元素記号,1,水素,H,
化学式,2,水酸化ナトリウム,NaOH,
化学式,3,酸化鉄(III)[赤さび],Fe2O3,
```

| 列 | 内容 | 備考 |
|---|---|---|
| `category` | `元素記号` または `化学式` | 記号だけでは判別不能なもの（O2 等）があるため明示必須 |
| `level` | 1〜9 の整数 | カテゴリごとに独立したレベル体系 |
| `name` | 日本語名称 | 補足（`(分子)` `[赤さび]` `(III)` 等）は**そのまま名称に含める**。特別扱いしない |
| `symbol` | 記号・化学式 | 添字は**半角数字**（`H2O`）。Unicode下付き（`H₂O`）で書いても取り込み時に正規化する。ローマ数字は半角 `(II)` |
| `note` | 予約列 | 全表示後に小さく添える補足。空でよい |

- **カードID = `category:name:symbol` の内容ベース**。行の並べ替え・追加・削除で学習履歴がズレない。名称や記号を直すとそのカードは新カード扱い（旧履歴は無害に残る）。
- 化学式レベルは Lv1〜4、元素記号は Lv1〜5（2026-09-20時点）。**後から追加・削除がある前提**。

### 2.2 owner書式からの変換 `tools/import-list.js`

owner が渡す自然な書式（`【レベル1】…` 見出し＋「名称 記号」を空白/タブ/`：`区切りで並べた行）を CSV に変換する。

```
node tools/import-list.js 元素記号 input.txt >> data/cards.csv
```

### 2.3 検証（ビルド前に必ず通る・失敗したらビルドしない）

- 化学式の元素記号が実在する（118元素の表と照合 → `Nh` のような誤字を弾く）
- 括弧の対応が取れている
- `(category, name, symbol)` の重複なし
- level が整数
- ヒント分割 → 結合 が元の文字列に戻る（トークナイザの健全性）

### 2.4 初期データ（2026-09-20 owner提供・訂正済み）

- 元素記号 Lv1 = 原子番号1〜20（20枚）／Lv2 = 7枚／Lv3 = 5枚／Lv4 = 6枚／Lv5 = 原子番号21〜36の残り7枚＋Mo,W,Sr,Cs,U（12枚）→ 計50枚
- 化学式 Lv1 = 13枚／Lv2 = 14枚／Lv3 = 17枚／Lv4 = 10枚（硫酸バリウム等・Claude提案分をowner承認で仮置き）→ 計54枚
- 訂正済み: 酸化鉄(III) Fe2O3（元データは(II)）／炭酸アンモニウム (NH4)2CO3（元データは NH4CO3）
- そのまま維持（owner判断）: エタノール CH3CH2OH／ヘリウム(分子) He 等の希ガス表記

## 3. 画面

### 3.1 起動時

- 範囲が1つ以上選択済みなら**トップを経由せず前回の続きのカードから始まる**（エンドレス・中断再開前提）。
- 未設定（初回）ならトップ。

### 3.2 トップ（設定・進捗・使い方が1画面に集約）

上から:

1. アプリ名 ＋ 使い方（3行以内。**カード画面には一切の説明文を出さない**＝一度読めばわかることを毎回見せない）
2. **全体概要バー**: 選択中の範囲×方向での「覚えた n / N」
3. **方向**: `記号→名称`（既定）／`名称→記号`／`ミックス`
4. **頻度調整**: ON／OFF（**既定 ON**・owner指示 2026-09-20）
5. **出題範囲**: カテゴリごとにレベルのチェックボックス（複数選択可）。各レベル行に**そのレベルの進捗バー（覚えた/総数）・「一覧」ボタン（→ §3.5 カード一覧）・リセットボタン**
6. 「はじめる／つづける」
7. 最下部: 「学習記録を全部リセット」（2段階確認）

### 3.3 カード

```
┌──────────────────────────┐
│ ‹ トップ            [最新へ ⇥] │ ← 「最新へ」は履歴を遡っている時だけ出る
├──────────────────────────┤
│                          │
│         NaOH             │ ← 問題面。タップ＝ヒントを1単位開く
│                          │
├─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┤
│                          │
│      水酸化＿＿＿＿          │ ← 答え面。タップ＝一発で全表示
│                          │
├──────────────────────────┤
│                 [ 覚えた ] │ ← 頻度調整ONのときだけ。右寄せ・幅いっぱいにしない（「次へ」に見せない）
└──────────────────────────┘
```

- **縦画面 = 上下**／**横画面 = 左右**（`orientation` メディアクエリで自動。設定項目にしない）
- 説明テキスト・プレースホルダは置かない（owner方針）。ただし未表示の答え面は「ここに答えが出る」と分かる程度の枠だけ持つ
- **ヒント**: 問題面をタップするごとに答えを左から1単位ずつ開く。未表示部分は単位数ぶんの下線 `＿` で長さだけ示す
- **答え**: 答え面タップで全表示。ヒント途中でも一発で全表示
- **次へ**:
  - 左スワイプ＝次へ（常時）
  - 答えが全表示の状態で答え面をもう一度タップ＝次へ（全表示から300msは無効＝連打の誤爆防止）
  - **進む操作（答え面再タップ・左スワイプ）は、頻度調整ONなら × として記録**（＝覚えていない。低努力の既定を×側に置く＝偽○を作らない。owner決定 2026-09-20）
  - **ボタンは「覚えた」1つだけ**（○記号なし・テキスト表記）。押すと ○ として記録して次へ。×ボタンは無い（進めば×）
  - **答えを見ずに「覚えた」を押してよい**（自己申告。見なくても分かる＝最も強い自信）
- **戻る**: 右スワイプで直前のカードへ（履歴50枚・ページを閉じたら消える）。戻ったカードは答え表示済みの状態で見る。**押し直し可（前の判定を取り消して上書き）**：×になっているカードに戻って「覚えた」を押せる
- `note` 列があれば全表示後に小さく添える

### 3.4 全部「覚えた」

頻度調整ONで選択範囲の全アイテムが「覚えた」になったら、カードの代わりに「この範囲は全部「覚えた」になりました」を表示し、トップへ戻す。トップでは範囲追加・リセット・頻度調整OFF（＝全部出る）のどれかを選べる。

### 3.5 カード一覧（別画面・1枚ずつ戻す）

トップの各レベル行「一覧」から開く。そのレベルの全カードを **覚えた → 記録あり → 未** の順に並べ、現在の方向での状態（覚えた／覚えた×1／—）を表示。「戻す」でそのカード（現在の方向）の記録を消して「未」に戻す。**覚えたカードは出なくなるので、1枚だけ戻す唯一の入口**。カード画面には何も足さない。

**用語**: 生徒向けUIでは「卒業」と言わず、**「覚えた」＝2回続けて○にしたカード＝出なくなる**、で統一する（owner指示 2026-09-20）。コード内のフィールド名 `grad` はそのまま。

## 4. ヒントの分割ルール（1本・例外なし）

> **1タップ＝次の1単位を開く。単位＝「元素記号（大文字＋続く小文字）」「連続する数字」「連続するローマ数字（I/V/X が2文字以上続く並び）」、それ以外は1文字。**

| 答え | 開き方 |
|---|---|
| H2O | H / H2 / H2O |
| Ca(OH)2 | Ca / Ca( / Ca(O / Ca(OH / Ca(OH) / Ca(OH)2 |
| C6H12O6 | C / C6 / C6H / C6H12 / C6H12O / C6H12O6 |
| 酸化鉄(III)[赤さび] | 酸 / 酸化 / 酸化鉄 / 酸化鉄( / 酸化鉄(III / 酸化鉄(III) / …[ / 赤 / さ / び / ] |
| ナトリウム | ナ / ナト / ナトリ / ナトリウ / ナトリウム |

- 単独の `I`（ヨウ素）・`V`（バナジウム）は元素記号として扱う。ローマ数字判定は**2文字以上の並び**のみ
- 表示時、化学式の数字は `<sub>` で下付きにする（データは半角のまま）

## 5. 出題アルゴリズム（`src/engine.js`・純関数・乱数注入）

### 5.1 アイテム

- **アイテム = カード × 方向**。`sn`（記号→名称）と `ns`（名称→記号）は独立に状態を持つ
- **統合**: 選択範囲内で同一方向・同一問題面のカードは1アイテムに統合し、答えを併記する
  - 併記の書式: 同カテゴリ内は `・`、カテゴリ間は `／` → `C` → `炭素／黒鉛・ダイヤモンド`、`Ag` → `銀`
  - 統合は**選択範囲の中でだけ**起きる（元素記号Lv1だけなら `C` → `炭素`）
  - 統合アイテムへの○×は構成カード全部に適用。重み＝構成カードの最大、「覚えた」＝構成カード全部が「覚えた」

### 5.2 状態（アイテムごと・localStorage）

```
{ w: 重み(初期1.0), streak: 連続○, day: streakを最後に増やした日(YYYY-MM-DD・フラグ用), grad: 「覚えた」フラグ, o,x: 累計 }
```

### 5.3 ○×

| 判定 | 重み | streak | 「覚えた」 |
|---|---|---|---|
| ○（「覚えた」ボタン） | ×0.5（下限0.1）＝出にくくなる | +1 | **2回続けて**（streak ≥ 2）で「覚えた」＝出なくなる |
| ×（進む操作） | **基準値 1.0 に戻す**（上げない） | 0 に戻す | 解除 |

- **×は出現頻度を上げない**（owner決定 2026-09-20）。×が既定操作である以上、×で重みを上げると全カードが上限に張り付いて差が消えるため。「○で下がる、×で元に戻る」の対称。結果、状態は実質3つ: **未(1.0)／覚えた×1(0.5)／覚えた(非表示)**
- 上限（旧8.0）は廃止。重みの範囲は [0.1, 1.0]
- `CONFIG.STREAK_ONCE_PER_DAY`（既定 **false**）を true にすると streak の加算が1日1回までになり、最低2日かかる。Claude が提案した制限だが **owner は承認しておらず**、2026-09-20 に「2回続けて○」が意図だと明示されたため既定OFF。「○にしたのに出るとうっとおしい」を避ける方針と整合
- 押し直し（戻って上書き）は「前の判定を巻き戻してから新判定を適用」。巻き戻しのため直前判定の前状態を履歴に保持する

### 5.4 次のカードの選び方（エンドレス）

1. 候補 = 選択範囲×方向のアイテム。頻度調整ONなら「覚えた」を除外（候補ゼロ→全部「覚えた」画面）
2. **クールダウン**: 直近に出した `floor(0.3 × 候補数)` 枚は出さない（候補が2枚以上なら最低1枚は出さない）
3. 頻度調整ON: 残りから**重み比例**で1枚（未1.0 : 覚えた×1 0.5）／OFF: **一様ランダム**で1枚
4. OFF中は○×を出さず、状態も一切変化しない（試験前の総チェック用）

## 6. 進捗表示

- 単位 = **レベル × 方向**。値 = 「覚えた」カード数 / そのレベルのカード数。表示ラベルは「覚えた n / N」
- トップ全体概要 = 選択範囲・選択方向での合計（ミックスは2方向ぶんを2本）
- リセット = **トップ**: レベル×方向単位（各バー横）＋全部（最下部・2段階確認）／**別画面（カード一覧）**: カード1枚×方向

## 7. オフライン・PWA

- Service Worker（stale-while-revalidate）: 一度開けば以後は圏外でも開ける。**絶対保証は謳わない**（「たぶん圏外でも動く」以上は言わない）
- 更新: リスト差し替え後の再デプロイは、生徒側では**次の次の起動**で反映（SWR の性質）
- `manifest.webmanifest`＋アイコン → 「ホーム画面に追加」で単独アプリ風に起動。未追加検知の初回案内はトップにだけ出す（閉じたら再表示しない）

## 8. 配布・インフラ

- **配信（現状・2026-09-20）**: Cloudflare **Pages** プロジェクト `kagaku-card` → **https://kagaku-card.pages.dev/**。アカウント `jiyuunatorao@gmail.com`
  - 本命は Workers（Static Assets）だったが、手元の API トークンが Pages:Edit スコープのみで Workers 経路が認証エラー → wrangler の案内どおり旧 Pages 経路（`--force` で作成）に置いた。静的ファイルだけなので Workers への移行は「Workers 権限つきトークンを1本作る → `wrangler deploy`」で完了する（生徒側のURLは変わるので配り直しが要る＝移行するなら早いうちに）
  - `wrangler.jsonc` は Workers 用に残してある。Pages デプロイは `tools/deploy.js` が dist を一時ディレクトリにコピーして実行する（同じ場所に wrangler.jsonc があると Pages が拒否するため）
- **ソース**: GitHub public リポジトリ **https://github.com/Torao-cos/kagaku-card**（2026-09-23 作成・push済み）。ホスティングには使わない。push は `tools/push.js`（リポ限定の fine-grained PAT を git 管理外ファイルから読む）
- **更新手順（1コマンド）**: `npm run ship` = テスト → 検証・ビルド → Cloudflare デプロイ → GitHub push。認証情報は git 管理外のファイル（`tools/deploy.js` / `tools/push.js` 冒頭参照）

## 9. やらないこと（初版）

- 4択・入力などのクイズ形式／正誤判定
- 成績のエクスポート・集計・サーバ保存
- SM-2型の間隔反復（「覚えた」＝完全omit、戻すのはリセットまたは頻度調整OFF）
- 音声読み上げ（将来候補）
- 化学反応式（係数・矢印）

## 10. 決定の経緯（要点）

- クイズ→フラッシュカードへ（owner・2026-09-19）: ハードル最小化。書く練習は授業側
- 方向既定＝記号→名称（owner）: 読める側から入る
- ヒントモードON/OFF廃止→タップ部位で使い分け（owner案・Claude提案の2ボタン案は不採用）
- クールダウン 5割→3割（owner）
- 「覚えた」=2回続けて○（owner）。1日1回制限はClaude提案→SPECに「owner承認」と誤記→2026-09-20 owner指示で撤回（フラグで残置）
- v1.1（2026-09-20 owner）: 頻度調整既定ON／○×の2択（△廃止）／答え面タップで次へ＝ON時は×扱い／「卒業」→「覚えた・出なくなる」表記／説明にタップ次へを追記
- v1.2（2026-09-20 owner）: ボタンは「覚えた」1つ／タップ・左スワイプ＝×／**×は重みを基準1.0に戻すだけで上げない**（owner案「×は頻度を変えない」＋Claude提案「○の割引は取り消す」）／トップ=全体・レベル別リセット、別画面=カード一覧から1枚ずつ戻す
- 1周・結果画面・枚数上限は**廃止**（owner: エンドレス・終わりなし）
- 進捗エクスポート廃止（Claude提案・owner承認）
- 「タップで答え」プレースホルダは不採用（owner: 毎回見せる説明は脳のリソースの無駄）
